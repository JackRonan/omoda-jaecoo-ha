# Standalone OMODA/Jaecoo API sandbox deps.
# Mirror of the HA integration's runtime deps (captcha solver needs numpy+pillow+pycryptodome),
# plus rich for the CLI and streamlit for the optional GUI (sandbox/gui.py).
requests>=2.31
pycryptodome>=3.20
numpy>=1.26
pillow>=10.0
rich>=13.0
streamlit>=1.30
